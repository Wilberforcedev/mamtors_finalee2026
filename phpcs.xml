<?xml version="1.0"?>
<ruleset name="MAM Tours Coding Standard">
    <description>Coding standard for MAM Tours Laravel application</description>

    <!-- Include the whole PSR-12 standard -->
    <rule ref="PSR12"/>

    <!-- Paths to check -->
    <file>app</file>
    <file>tests</file>

    <!-- Paths to ignore -->
    <exclude-pattern>app/Console/Kernel.php</exclude-pattern>
    <exclude-pattern>app/Http/Kernel.php</exclude-pattern>
    <exclude-pattern>bootstrap</exclude-pattern>
    <exclude-pattern>storage</exclude-pattern>
    <exclude-pattern>vendor</exclude-pattern>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Use colors -->
    <arg name="colors"/>

    <!-- Show sniff codes in all reports -->
    <arg value="s"/>

    <!-- Additional rules -->
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Generic.Commenting.Todo"/>
    <rule ref="Generic.ControlStructures.InlineControlStructure"/>
    <rule ref="Generic.Formatting.DisallowMultipleStatements"/>
    <rule ref="Generic.Functions.FunctionCallArgumentSpacing"/>
    <rule ref="Generic.NamingConventions.UpperCaseConstantName"/>
    <rule ref="Generic.PHP.LowerCaseConstant"/>
    <rule ref="Generic.PHP.LowerCaseKeyword"/>
    <rule ref="Generic.Strings.UnnecessaryStringConcat"/>
    <rule ref="Generic.WhiteSpace.DisallowTabIndent"/>

    <!-- Exclude some rules that conflict with Laravel conventions -->
    <rule ref="PSR1.Classes.ClassDeclaration.MissingNamespace">
        <exclude-pattern>database/migrations/*</exclude-pattern>
        <exclude-pattern>database/seeders/*</exclude-pattern>
    </rule>
</ruleset>